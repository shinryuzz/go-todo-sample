version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: /bin/sh -c "go run main.go"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"
